<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Blogs</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="column-2">
                <form method="POST" style="position: relative; left: 0; top:0; padding: 10px;" action="/club_head/" >
                 <button type="submit" class='btn btn-outline-info btn-sm'>HOME</button> 
                 </form>        
            </div>

        </div>
    </div>
    <div class="container">
    <% if (blogs && blogs.length) { %>
    <h2 style="color: blue; text-decoration: underline;"> List of All Blogs </h2>
    <!-- Template for each blog-->
    <div class="card-group">
        <% blogs.forEach((blog) => { %>
            <div class="card" style="width: 18rem;">

                
                <img src="/images/<%= blog.poster_url %>" alt="thumbnail of poster" width="286px">
                <div class="card-body">
                    <!-- <h5 class="card-title"><%= blog.name %></h5> -->
                    <p>
                        <div class="card border-dark mb-3">
                            <div class="card-header">Event Summary</div>
                            <div class="card-body text-dark">
                              <h5 class="card-title">Gallery</h5>
                              <p class="card-text">
                                <ul class="list-group list-group-flush" >
                                    <div style="max-height: 150px; overflow-y: scroll;">
                                    <% if ((blog.gallery) && (blog.gallery).length) { %>
                                    <% (blog.gallery).forEach((url) => { %>
                                    <li class="list-group-item"><img src="/image/<%= url %>"></img></li>
                                    
                                    <% }) %>
                                    <% }else{ %>
                                        <li class="list-group-item">NULL</li>
                                    <% }%>
                                    </div>
                                </ul>
                              </p>
                              <h5 class="card-title">Reference Links</h5>
                              <p class="card-text">
                                <ul class="list-group list-group-flush" >
                                    <div style="max-height: 150px; overflow-y: scroll;">
                                    <% if ((blog.outside_links) && (blog.outside_links).length) { %>
                                    <% (blog.outside_links).forEach((url) => { %>
                                    <li class="list-group-item"><a href="<%= url %>"><%= url %></a></li>
                                    
                                    <% }) %>
                                    <% }else{ %>
                                        <li class="list-group-item">NULL</li>
                                    <% }%>
                                    </div>
                                </ul>
                              </p>
                              <h5 class="card-title">Attachments</h5>
                              <p class="card-text">
                                <ul class="list-group list-group-flush" >
                                    <div style="max-height: 150px; overflow-y: scroll;">
                                    <% if ((blog.file_attachment) && (blog.file_attachment).length) { %>
                                    <% (blog.file_attachment).forEach((url) => { %>
                                    <li class="list-group-item"><a href="<%= url %>"><%= url %></a></li>
                                    
                                    <% }) %>
                                    <% }else{ %>
                                        <li class="list-group-item">NULL</li>
                                    <% }%>
                                    </div>
                                </ul>
                              </p>
                            </div>
                          </div>
                    </p>

                    
                    <p>
                    <a href="/blog/update/<%=blog._id%>">edit</a>  |  <a href="/blog/delete/<%=blog._id%>">delete</a>
                    </p>
                </div>
            </div>
        <% }) %>
    </div>
    <% }else{ %>
        <div class="container">
            <h1 class="display-1">No blog created yet</h1>
        </div>
    <% }%>
    <div style="text-align: center; margin-top: 20px; margin-bottom: 30px;">
        <a href="/blog/create" ><span class="material-icons">add</span>Create a Blog</a>
    </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.4.1.slim.min.js" integrity="sha384-J6qa4849blE2+poT4WnyKhv5vZF5SrPo0iEjwBvKU7imGFAV0wwj1yYfoRSJoZ+n" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>
</body>
</html>